(function(){
  const $=(s,p=document)=>p.querySelector(s), $$=(s,p=document)=>[...p.querySelectorAll(s)];
  const esc=(v='')=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  let editingId=null;
  function inject(){const form=$('#productAdminForm');if(!form||$('#productEnhancedFields'))return;const block=document.createElement('div');block.id='productEnhancedFields';block.innerHTML=`<div class="form-grid" style="margin-top:14px;padding-top:14px;border-top:1px solid var(--line)"><div class="field full"><label>產品圖片路徑／URL</label><input id="admin_image" placeholder="assets/images/products/example.jpg 或 https://..."></div><div class="field full"><label>產品特色（每行一項）</label><textarea id="admin_highlights" placeholder="高解析度列印&#10;工業級耐用機構&#10;支援多種連線"></textarea></div><div class="field full"><label>產品規格（每行：規格名稱 = 規格內容）</label><textarea id="admin_specs" placeholder="解析度 = 203 / 300 dpi&#10;最大列印寬度 = 104 mm"></textarea></div><div class="field full"><label>產品文件（每行：類型 | 名稱 | URL，可不填 URL）</label><textarea id="admin_files" placeholder="PDF | 產品型錄 | https://...&#10;PDF | 使用手冊 | https://..."></textarea></div></div>`;const check=form.querySelector('.check-field');form.insertBefore(block,check)}
  function fill(id){const d=FBStore.getData(),p=d.products.find(x=>x.id===id);if(!p)return;editingId=id;$('#admin_image').value=p.image||'';$('#admin_highlights').value=(p.highlights||[]).join('\n');$('#admin_specs').value=(p.specs||[]).map(x=>`${x[0]} = ${x[1]}`).join('\n');$('#admin_files').value=(p.files||[]).map(f=>`${f.type||'PDF'} | ${f.label||''} | ${f.url||''}`).join('\n')}
  function clear(){editingId=null;['#admin_image','#admin_highlights','#admin_specs','#admin_files'].forEach(s=>{if($(s))$(s).value=''})}
  function parseSpecs(v){return v.split('\n').map(x=>x.trim()).filter(Boolean).map(line=>{const i=line.indexOf('=');return i<0?[line,'']:[line.slice(0,i).trim(),line.slice(i+1).trim()]})}
  function parseFiles(v){return v.split('\n').map(x=>x.trim()).filter(Boolean).map(line=>{const p=line.split('|').map(x=>x.trim());return {type:p[0]||'PDF',label:p[1]||p[0]||'文件',url:p[2]||''}})}
  function persist(){const id=$('#admin_id')?.value.trim();if(!id)return;setTimeout(()=>{const d=FBStore.getData(),p=d.products.find(x=>x.id===id);if(!p)return;p.image=$('#admin_image').value.trim();p.highlights=$('#admin_highlights').value.split('\n').map(x=>x.trim()).filter(Boolean);p.specs=parseSpecs($('#admin_specs').value);p.files=parseFiles($('#admin_files').value);FBStore.saveData(d)},0)}
  document.addEventListener('DOMContentLoaded',()=>{if(document.body.dataset.page!=='admin')return;inject();document.addEventListener('click',e=>{const edit=e.target.closest('.edit-product');if(edit)setTimeout(()=>fill(edit.dataset.id),0);if(e.target.closest('#newProductBtn'))setTimeout(clear,0)});$('#productAdminForm')?.addEventListener('submit',persist,true);window.addEventListener('farbeyound:datachange',()=>{if(editingId&&$('#adminFormPanel')?.classList.contains('open'))setTimeout(()=>fill(editingId),20)})})
})();