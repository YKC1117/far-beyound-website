(()=>{
  if(document.body.dataset.page!=='admin'||window.__fbAdminSecurityPolicyUI)return;
  window.__fbAdminSecurityPolicyUI=true;
  const ENDPOINT='https://papqrnqbfauwuipjwwdh.supabase.co/functions/v1/admin-security-policy';
  const $=s=>document.querySelector(s);
  async function call(method='GET',body){const s=window.FBAdminAuth?.session?.();if(!s?.access_token)throw new Error('login_required');const r=await fetch(ENDPOINT,{method,headers:{Authorization:`Bearer ${s.access_token}`,'Content-Type':'application/json'},body:body?JSON.stringify(body):undefined,cache:'no-store'}),b=await r.json().catch(()=>({}));if(!r.ok)throw new Error(b.error||'request_failed');return b}
  function host(){return document.querySelector('#adminSecurityCenter .admin-system-body')}
  function build(){const h=host();if(!h||$('#adminSecurityPolicyBox'))return;const d=document.createElement('details');d.id='adminSecurityPolicyBox';d.className='admin-tool-block';d.innerHTML='<summary style="cursor:pointer"><b>資安政策設定</b> <span class="admin-fold-indicator">展開設定 ＋</span><small style="display:block;margin-top:4px;color:#70848f">登入限制、逾時、備份保留與稽核保留。平常不需要展開。</small></summary><div id="adminSecurityPolicyBody" style="padding-top:14px"><div class="admin-tool-empty">登入具備「資安管理」權限的個人帳號後可讀取。</div></div>';h.prepend(d);d.addEventListener('toggle',()=>{const i=d.querySelector('.admin-fold-indicator');if(i)i.textContent=d.open?'收合設定 −':'展開設定 ＋';if(d.open)load()})}
  function field(id,label,min,max,unit,note=''){return `<div class="field"><label>${label}</label><input id="${id}" type="number" min="${min}" max="${max}" step="1"><small>${unit}${note?`｜${note}`:''}</small></div>`}
  async function load(){const body=$('#adminSecurityPolicyBody');if(!body)return;body.innerHTML='<div class="admin-tool-empty">正在讀取資安政策…</div>';try{const b=await call(),p=b.policy||{};body.innerHTML=`<div class="admin-danger-note"><b>系統級設定</b>兩步驟驗證與敏感操作重新驗證目前固定啟用，不提供一般關閉開關。調低鎖定或保留規則會影響公司後台安全。</div><div class="form-grid">${field('secMaxFail','登入失敗上限',3,10,'次','預設 5 次')}${field('secFailWindow','失敗判斷時間窗',5,120,'分鐘','預設 15 分鐘')}${field('secLockout','暫時鎖定時間',5,1440,'分鐘','預設 30 分鐘')}${field('secIdle','登入閒置逾時',10,480,'分鐘','預設 60 分鐘')}${field('secBackupKeep','自動備份保留數',10,1000,'份','超過後由最舊自動備份開始輪替')}${field('secAuditDays','稽核紀錄保留',30,3650,'天','登入與操作紀錄同時套用')}</div><div class="admin-usage-note" style="margin:14px 0"><b>固定安全規則：</b>二維碼 2FA＝開啟｜敏感操作重新驗證＝開啟。基準版本、手動保留、還原前保護不受自動備份份數限制。</div><button id="adminSecurityPolicySave" class="btn btn-primary btn-sm" type="button">儲存資安政策</button><span id="adminSecurityPolicyStatus" class="admin-save-state" style="margin-left:8px">尚未修改</span>`;const map={secMaxFail:p.max_failed_attempts,secFailWindow:p.failed_window_minutes,secLockout:p.lockout_minutes,secIdle:p.session_idle_minutes,secBackupKeep:p.backup_retention_count,secAuditDays:p.audit_retention_days};Object.entries(map).forEach(([id,v])=>{const el=$('#'+id);if(el)el.value=String(v??'')});$('#adminSecurityPolicySave').onclick=save}catch(e){body.innerHTML=`<div class="admin-tool-empty">${e.message==='login_required'?'請先登入個人帳號。':e.message==='mfa_required'?'請先完成二維碼兩步驟驗證。':'目前帳號沒有資安管理權限，或政策暫時無法讀取。'}</div>`}}
  async function save(){const btn=$('#adminSecurityPolicySave'),status=$('#adminSecurityPolicyStatus');const pass=await window.FBAdminReauth?.ask?.('重新驗證｜資安政策','這會改變整個後台的登入、逾時與紀錄保留規則。請輸入你目前的個人帳號密碼。');if(!pass)return;btn.disabled=true;btn.textContent='儲存中…';try{await call('POST',{current_password:pass,max_failed_attempts:Number($('#secMaxFail').value),failed_window_minutes:Number($('#secFailWindow').value),lockout_minutes:Number($('#secLockout').value),session_idle_minutes:Number($('#secIdle').value),backup_retention_count:Number($('#secBackupKeep').value),audit_retention_days:Number($('#secAuditDays').value)});status.textContent='已儲存';status.classList.remove('dirty');alert('資安政策已更新。新的登入與保留規則會立即套用。');await load()}catch(e){status.textContent=e.message==='reauth_failed'?'個人密碼驗證失敗':'儲存失敗';status.classList.add('dirty');alert(e.message==='mfa_required'?'請先完成二維碼兩步驟驗證。':e.message==='reauth_failed'?'個人帳號密碼驗證失敗，未修改資安政策。':'資安政策儲存失敗，請確認權限後再試。')}finally{btn.disabled=false;btn.textContent='儲存資安政策'}}
  function init(){build()}document.readyState==='loading'?document.addEventListener('DOMContentLoaded',()=>[2000,3200].forEach(t=>setTimeout(init,t)),{once:true}):[1100,2400].forEach(t=>setTimeout(init,t))
})();