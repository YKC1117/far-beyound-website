/* Legacy compatibility shim.
 * The public site now uses quick-contact-final.js as the single quick-contact controller.
 * Kept as a no-op because several legacy HTML pages still reference this filename.
 */
(function(){'use strict';
  window.__fbDesktopPhonePopoverLegacyDisabled = true;
})();
