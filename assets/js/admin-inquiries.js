(()=>{
  if(document.body.dataset.page!=='admin')return;
  const ENDPOINT='https://papqrnqbfauwuipjwwdh.supabase.co/functions/v1/admin-inquiries';
  const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const fmt=v=>{try{return new Date(v).toLocaleString('zh-TW',{hour12:false})}catch(_){return String(v||'')}};
  let lastRows=[],legacyPending=null;
  function legacyPassword(){
    if(legacyPending)return legacyPending;
    legacyPending=new Promise(resolve=>{
      let root=document.getElementById('inquiryLegacyAuth');
      if(!root){const st=document.createElement('style');st.textContent='.inquiry-legacy-auth{position:fixed;inset:0;z-index:10070;display:none;place-items:center;padding:18px;background:rgba(7,24,45,.62);backdrop-filter:blur(5px)}.inquiry-legacy-auth.open{display:grid}.inquiry-legacy-card{width:min(420px,100%);padding:20px;border-radius:16px;background:#fff;box-shadow:0 26px 70px rgba(0,0,0,.28)}.inquiry-legacy-card h3{margin:0 0 6px}.inquiry-legacy-card p{margin:0 0 14px;color:#6c808c;font-size:12px;line-height:1.6}.inquiry-legacy-card input{box-sizing:border-box;width:100%;min-height:44px;padding:10px 12px;border:1px solid #cfdde3;border-radius:10px;font-size:16px}.inquiry-legacy-actions{display:flex;gap:8px;margin-top:12px}.inquiry-legacy-actions .btn{flex:1}';document.head.appendChild(st);root=document.createElement('div');root.id='inquiryLegacyAuth';root.className='inquiry-legacy-auth';root.innerHTML='<form class="inquiry-legacy-card"><h3>查看詢問資料</h3><p>首位 Owner 尚未建立時可暫用共用後台密碼。密碼只用於本次讀取，不會存入瀏覽器。</p><input type="password" autocomplete="current-password" aria-label="後台發布密碼" required maxlength="256"><div class="inquiry-legacy-actions"><button class="btn btn-primary" type="submit">驗證並讀取</button><button class="btn btn-secondary" type="button">取消</button></div></form>';document.body.appendChild(root)}
      const form=root.querySelector('form'),input=root.querySelector('input'),cancel=root.querySelector('button[type="button"]');const done=v=>{input.value='';root.classList.remove('open');form.onsubmit=null;cancel.onclick=null;resolve(v)};form.onsubmit=e=>{e.preventDefault();done(input.value||'')};cancel.onclick=()=>done('');root.classList.add('open');setTimeout(()=>input.focus(),20)
    }).finally(()=>{legacyPending=null});return legacyPending
  }
  function statusLabel(v){return v==='sent'?'已寄出':v==='failed'?'寄送失敗':'待處理'}
  function render(){const body=document.getElementById('inquiryRows');if(!body)return;const q=(document.getElementById('inquirySearch')?.value||'').trim().toLowerCase(),state=document.getElementById('inquiryEmailFilter')?.value||'';const rows=lastRows.filter(x=>{if(state&&String(x.email_status||'')!==state)return false;if(!q)return true;return[x.company,x.department,x.contact_name,x.job_title,x.subject,x.budget,x.phone,x.extension,x.mobile,x.email,x.message].some(v=>String(v||'').toLowerCase().includes(q))});const count=document.getElementById('inquiryVisibleCount');if(count)count.textContent=`顯示 ${rows.length}／${lastRows.length} 筆`;if(!rows.length){body.innerHTML=`<tr><td colspan="6">${lastRows.length?'沒有符合目前搜尋／篩選條件的詢問。':'目前還沒有網站詢問紀錄。'}</td></tr>`;return}body.innerHTML=rows.map(x=>`<tr><td>${esc(fmt(x.created_at))}</td><td><b>${esc(x.company)}</b><br><span>${esc(x.department||'')} ${esc(x.contact_name||'')}${x.job_title?'／'+esc(x.job_title):''}</span></td><td>${esc(x.subject||'一般詢問')}${x.budget?'<br><span>'+esc(x.budget)+'</span>':''}</td><td>${esc(x.phone||'')}${x.extension?' 分機 '+esc(x.extension):''}${x.mobile?'<br>'+esc(x.mobile):''}<br><a href="mailto:${esc(x.email||'')}">${esc(x.email||'')}</a></td><td><b>${esc(statusLabel(x.email_status))}</b></td><td style="min-width:220px;white-space:normal">${esc(x.message||'—')}</td></tr>`).join('')}
  function build(){if(document.getElementById('adminInquiries'))return;const anchor=document.getElementById('adminAnalytics')||document.getElementById('products');if(!anchor){setTimeout(build,100);return}const box=document.createElement('section');box.id='adminInquiries';box.className='admin-panel';box.innerHTML='<div class="admin-panel-head"><div><span class="eyebrow">CUSTOMER INQUIRIES</span><h2>網站詢問紀錄</h2><p>客戶表單會先安全留存在資料庫；正式個人帳號建立後，只允許具備「網站詢問」權限的人員讀取。</p></div><div style="display:flex;gap:8px;flex-wrap:wrap"><button id="inquiryLoad" class="btn btn-primary btn-sm" type="button">讀取最新詢問</button><button id="inquiryAccess" class="btn btn-secondary btn-sm" type="button">帳號／權限</button></div></div><div id="inquiryStatus" class="admin-usage-note">尚未讀取。客戶聯絡資料屬敏感資訊，不會在頁面載入時自動讀取。</div><div class="admin-product-tools" style="margin:0 18px 12px"><input id="inquirySearch" type="search" placeholder="搜尋公司、姓名、電話、Email、內容…" aria-label="搜尋網站詢問"><select id="inquiryEmailFilter" aria-label="Email 通知狀態"><option value="">全部通知狀態</option><option value="sent">已寄出</option><option value="failed">寄送失敗</option></select><span id="inquiryVisibleCount" class="admin-product-count">尚未讀取</span></div><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>時間</th><th>公司／聯絡人</th><th>洽詢項目</th><th>聯絡方式</th><th>Email 通知</th><th>內容</th></tr></thead><tbody id="inquiryRows"><tr><td colspan="6">尚未讀取資料。</td></tr></tbody></table></div>';anchor.after(box);document.getElementById('inquiryLoad').onclick=load;document.getElementById('inquiryAccess').onclick=()=>{const x=document.getElementById('adminAccessCenter');if(x?.tagName==='DETAILS')x.open=true;x?.scrollIntoView({behavior:'smooth',block:'start'})};document.getElementById('inquirySearch').addEventListener('input',render);document.getElementById('inquiryEmailFilter').addEventListener('change',render)}
  async function load(){const status=document.getElementById('inquiryStatus'),body=document.getElementById('inquiryRows');status.textContent='正在確認權限並讀取最新詢問…';try{const headers={};let s=window.FBAdminAuth?.session?.();if(s?.access_token){const refreshed=await window.FBAdminAuth.refresh?.().catch(()=>null);s=refreshed||window.FBAdminAuth.session?.()||s;headers.Authorization=`Bearer ${s.access_token}`}else{const pass=await legacyPassword();if(!pass){status.textContent='已取消讀取。';return}headers['x-admin-pass']=pass}const r=await fetch(ENDPOINT+'?limit=100',{headers,cache:'no-store'}),payload=await r.json().catch(()=>null);if(!r.ok){const err=new Error(payload?.error||'read');throw err}lastRows=Array.isArray(payload)?payload:[];render();status.textContent=lastRows.length?`已讀取 ${lastRows.length} 筆最新詢問。可使用下方搜尋與通知狀態篩選。`:'已讀取，目前沒有詢問紀錄。'}catch(e){lastRows=[];const msg=e.message==='personal_login_required'?'已啟用個人後台帳號，請先登入並確認具備「網站詢問」權限。':e.message==='forbidden'?'目前登入帳號沒有「網站詢問」權限。':e.message==='unauthorized'?'驗證失敗，未讀取任何客戶資料。':'詢問資料讀取失敗，請稍後再試。';status.textContent=msg;body.innerHTML='<tr><td colspan="6">目前無法載入詢問資料。</td></tr>';const count=document.getElementById('inquiryVisibleCount');if(count)count.textContent='讀取失敗'}}
  document.readyState==='loading'?document.addEventListener('DOMContentLoaded',build,{once:true}):build();
})();
